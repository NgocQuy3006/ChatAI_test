from flask import Blueprint, request, jsonify, session, current_app, url_for
from datetime import datetime
import os
import requests
from dotenv import load_dotenv
from db import SessionLocal
from models import Conversation, Message
from werkzeug.utils import secure_filename

# =======================================================
# ⚙️ Cấu hình API
# =======================================================
load_dotenv()
print("🔑 OPENROUTER_API_KEY =", os.getenv("OPENROUTER_API_KEY"))
OPENROUTER_API_KEY = os.getenv("OPENROUTER_API_KEY")
API_URL = "https://openrouter.ai/api/v1/chat/completions"

chat_bp = Blueprint("chat_bp", __name__)

# =======================================================
# 💬 Chat API chính
# =======================================================
@chat_bp.route("/chat", methods=["POST"])
def chat():
    user_msg = request.json.get("message", "").strip()
    is_new_chat = request.json.get("new_chat", False)
    if not user_msg:
        return jsonify({"error": "Message cannot be empty"}), 400

    db = SessionLocal()
    conv_id = session.get("conversation_id")

    # Nếu là chat mới → đóng cuộc cũ
    if is_new_chat and conv_id:
        old_conv = db.query(Conversation).get(conv_id)
        if old_conv:
            old_conv.ended_at = datetime.now()
        session.pop("conversation_id", None)
        session.pop("history", None)
        db.commit()

    # Nếu chưa có cuộc hội thoại → tạo mới
    if not session.get("conversation_id"):
        new_conv = Conversation()
        db.add(new_conv)
        db.commit()
        session["conversation_id"] = new_conv.id
        session["history"] = []

    conv_id = session["conversation_id"]
    history = session["history"]

    # Lưu tin nhắn user
    user_msg_db = Message(role="user", content=user_msg, conversation_id=conv_id)
    db.add(user_msg_db)
    db.commit()

    history.append({
        "role": "user",
        "content": user_msg,
        "timestamp": datetime.now().strftime("%H:%M:%S")
    })

    # 🧠 Gọi OpenRouter API
    try:
        short_history = history[-10:]  # giữ lịch sử gọn nhẹ

        headers = {
            "Authorization": f"Bearer {OPENROUTER_API_KEY}",
            "Content-Type": "application/json",
            "Referer": "http://localhost:5000",  # ⚠️ Sửa đúng key Referer
            "X-Title": "My GPT App"
        }

        data = {
            "model": "openai/gpt-4o-mini",  # ✅ model nhẹ, ổn định
            "max_tokens": 600,
            "messages": (
                [{"role": "system", "content": "You are a helpful assistant."}]
                + [{"role": m["role"], "content": m["content"]} for m in short_history]
            ),
        }

        res = requests.post(API_URL, headers=headers, json=data, timeout=60)
        print("🔍 API response:", res.status_code, res.text)
        res_json = res.json()

        # ✅ Nếu API không trả về kết quả hợp lệ
        if "choices" not in res_json:
            print("❌ Lỗi API:", res_json)
            ai_reply = (
                f"Lỗi từ OpenRouter ({res.status_code}): "
                f"{res_json.get('error', {}).get('message', str(res_json))}"
            )
        else:
            ai_reply = res_json["choices"][0]["message"]["content"].strip()

    except requests.exceptions.RequestException as e:
        ai_reply = f"Lỗi mạng khi gọi API: {e}"

    except Exception as e:
        ai_reply = f"Lỗi khi gọi API: {e}"

    # Lưu phản hồi AI
    ai_msg_db = Message(role="assistant", content=ai_reply, conversation_id=conv_id)
    db.add(ai_msg_db)
    db.commit()

    history.append({
        "role": "assistant",
        "content": ai_reply,
        "timestamp": datetime.now().strftime("%H:%M:%S")
    })
    session["history"] = history
    session.modified = True

    db.close()
    return jsonify({"history": history, "conversation_id": conv_id})


# =======================================================
# 📜 Danh sách các cuộc hội thoại
# =======================================================
@chat_bp.route("/conversations", methods=["GET"])
def list_conversations():
    db = SessionLocal()
    conversations = db.query(Conversation).order_by(Conversation.id.desc()).all()
    data = [
        {
            "id": c.id,
            "title": f"Cuộc trò chuyện #{c.id}",
            "created_at": c.created_at.strftime("%H:%M:%S"),
            "ended_at": c.ended_at.strftime("%H:%M:%S") if c.ended_at else None,
        }
        for c in conversations
    ]
    db.close()
    return jsonify(data)


# =======================================================
# 💾 Lấy toàn bộ tin nhắn của 1 cuộc hội thoại
# =======================================================
@chat_bp.route("/messages/<int:conv_id>", methods=["GET"])
def get_messages(conv_id):
    db = SessionLocal()
    messages = db.query(Message).filter(Message.conversation_id == conv_id).order_by(Message.id).all()
    data = [{"role": m.role, "content": m.content, "timestamp": m.timestamp.strftime("%H:%M:%S")} for m in messages]
    db.close()
    return jsonify(data)


# =======================================================
# 🖼️ Upload + hỏi ảnh (Vision)
# =======================================================
ALLOWED_EXT = {"png", "jpg", "jpeg"}

def allowed_file(filename):
    return "." in filename and filename.rsplit(".", 1)[1].lower() in ALLOWED_EXT


@chat_bp.route("/upload-image", methods=["POST"])
def upload_image():
    if "image" not in request.files:
        return jsonify({"error": "Không có file"}), 400
    f = request.files["image"]
    if f.filename == "":
        return jsonify({"error": "Chưa chọn file"}), 400
    if not allowed_file(f.filename):
        return jsonify({"error": "Chỉ chấp nhận PNG/JPG"}), 400

    filename = secure_filename(f.filename)
    timestamp = datetime.now().strftime("%Y%m%d%H%M%S%f")
    name, ext = os.path.splitext(filename)
    filename = f"{name}_{timestamp}{ext}"
    save_path = os.path.join(current_app.config["UPLOAD_FOLDER"], filename)
    f.save(save_path)

    session["uploaded_image"] = filename
    session.modified = True
    img_url = url_for("static", filename=f"uploads/{filename}", _external=True)
    return jsonify({"filename": filename, "url": img_url})


@chat_bp.route("/image-question", methods=["POST"])
def image_question():
    question = (request.json or {}).get("question", "").strip()
    if not question:
        return jsonify({"error": "Câu hỏi không được để trống"}), 400

    filename = session.get("uploaded_image")
    if not filename:
        return jsonify({"error": "Chưa upload ảnh nào"}), 400

    # Đường dẫn thật của ảnh
    img_path = os.path.join(current_app.config["UPLOAD_FOLDER"], filename)

    # Chuyển ảnh sang base64 để OpenRouter xử lý trực tiếp
    import base64
    with open(img_path, "rb") as f:
        img_b64 = base64.b64encode(f.read()).decode("utf-8")

    db = SessionLocal()
    conv_id = session.get("conversation_id")
    if not conv_id:
        new_conv = Conversation()
        db.add(new_conv)
        db.commit()
        session["conversation_id"] = new_conv.id
        session["history"] = []
        conv_id = new_conv.id

    # Lưu câu hỏi người dùng
    user_msg = Message(role="user", content=f"[Hỏi ảnh] {question}", conversation_id=conv_id)
    db.add(user_msg)
    db.commit()

    try:
        headers = {
            "Authorization": f"Bearer {OPENROUTER_API_KEY}",
            "Content-Type": "application/json",
            "Referer": "http://localhost:5000",
            "X-Title": "My GPT App",
        }

        data = {
            "model": "openai/gpt-4o-mini",  # ✅ nhẹ, ổn định hơn gpt-4o
            "max_tokens": 800,
            "messages": [
                {
                    "role": "user",
                    "content": [
                        {"type": "text", "text": question},
                        {
                            "type": "image_url",
                            "image_url": {
                                "url": f"data:image/jpeg;base64,{img_b64}"
                            },
                        },
                    ],
                }
            ],
        }

        res = requests.post(API_URL, headers=headers, json=data, timeout=60)
        res_json = res.json()

        if "choices" not in res_json:
            print("❌ Lỗi API:", res_json)
            answer = f"Lỗi API trả về: {res_json.get('error', {}).get('message', res_json)}"
        else:
            answer = res_json["choices"][0]["message"]["content"].strip()

    except Exception as e:
        answer = f"Lỗi khi gọi API: {e}"

    # Lưu phản hồi AI
    ai_msg = Message(role="assistant", content=f"[Ảnh] {answer}", conversation_id=conv_id)
    db.add(ai_msg)
    db.commit()
    db.close()

    # URL ảnh để hiển thị lại trong frontend
    img_url = url_for("static", filename=f"uploads/{filename}", _external=True)

    return jsonify({"reply": answer, "image_url": img_url})
